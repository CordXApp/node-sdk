export interface InstatusOptions {
    apiKey: string
    apiVersion: string
    pageId: string
    compId: string
}

export interface DefaultOptions {
    defaultURL: 'https://api.instatus.com'
    defaultHelpUrl: 'https://instatus.com/help/api/'
    defaultVersion: 'v2'
}
